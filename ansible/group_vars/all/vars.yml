---
# 모든 서버에 공통 적용되는 변수
# 비밀번호/API 키는 vault.yml
# 실제 환경 IP/DNS는 local.yml

# ── Django ALLOWED_HOSTS ──────────────────────────────────────────
django_allowed_hosts:
  - "*"

# ── 환경 의존 변수 ────────────────────────────────────────────────
# 아래 변수는 group_vars/all/local.yml 에서 정의합니다.
#
# django_private_ip
# nginx_private_ip
# internal_alb_dns
# efs_dns

# ── DB ────────────────────────────────────────────────────────────
# django_db_host 역시 group_vars/all/local.yml 에서 정의합니다.
